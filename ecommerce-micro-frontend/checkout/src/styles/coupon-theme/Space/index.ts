import { couponSpacingTokens } from '../../coupon-tokens';

export const Space: Record<string, any> = {
  // Space component doesn't have many overrideable tokens
  // Most spacing is controlled via the size prop
};

