# Workspace — agent guidance

Cross-cutting guidance for agents working anywhere in this repo. Part-specific guidance
belongs in the relevant `workspace/_<part>/AGENT.md`.

## Conventions (complete at onboarding)

- Branching: `myteam/<feature-kebab>`; conventional commits.
- Quality gates: see `.cursor/myteam/config.yaml > defaults.qualityGates`.
- Agent roles: see `.cursor/agents/` and the registry in `config.yaml`.

## Reusable patterns

_(Promote general, reusable patterns here as they are discovered. Keep it short and durable —
story-specific notes go in the PRD's `progress.txt`.)_
