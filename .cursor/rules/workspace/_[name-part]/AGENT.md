# _[name-part] — template part

Copy this directory to `_<your-part>/` (e.g. `_services/`) and fill in the two files:
`AGENT.md` (this file) and `_[name-part].mdc` (rename to `<part>.mdc`).

## What is this part?

_One paragraph: what this part of the workspace is and why it exists._

## Where it lives

_Repo path(s), e.g. `Services/`._

## Run / build / test

```bash
# how to run, build, and test this part locally
```

## Patterns

_(Reusable patterns and conventions for this part.)_

## Gotchas

_(Non-obvious requirements, dependencies between files, env/config needs, testing approach.)_

## Owners / agents

_Which agent roles typically work here (see `.cursor/agents/`), e.g. backend-architect, api-tester._
